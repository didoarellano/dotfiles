# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below. ISO 10646 = Unicode
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, if you need a lot of unicode glyphs or
# right-to-left text rendering, you should instead use pango for rendering and
# chose a FreeType font, such as:
font pango:Fira Mono Medium 8

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
# bindsym $mod+Return exec i3-sensible-terminal
# bindsym $mod+Return exec kitty
# bindsym $mod+ctrl+Mod1+Return exec roxterm --separate --profile=dark
# bindsym $mod+Shift+Return exec emacsclient --create-frame --alternate-editor="" --eval "(--initial-frame-setup)"

# kill focused window
bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
# bindsym $mod+d exec dmenu_run
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym Mod4+space exec --no-startup-id i3-dmenu-desktop
# bindsym Mod4+space exec dmenu_run -nb "#000000" -nf "#cccccc" -sb "#4c7899"
# bindsym Mod1+space exec bashrun2 toggle
# bindsym Mod1+space exec --no-startup-id zsh -c "responsive-rofi run"
# bindsym Mod1+ctrl+space exec --no-startup-id zsh -c "responsive-rofi window"

# Turn off focus follows mouse
focus_follows_mouse no

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# Horizontal and vertical splits the VIM way
bindsym $mod+s split v
bindsym $mod+v split h

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen
bindsym $mod+Shift+f fullscreen global

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+t layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+p focus parent

# focus the child container
bindsym $mod+c focus child

# Cycle through workspaces
bindsym $mod+grave workspace back_and_forth
bindsym $mod+Tab workspace next
bindsym $mod+Shift+Tab workspace prev

# switch to workspace
bindsym $mod+1 workspace 1
bindsym $mod+2 workspace 2
bindsym $mod+3 workspace 3
bindsym $mod+4 workspace 4
bindsym $mod+5 workspace 5
bindsym $mod+6 workspace 6
bindsym $mod+7 workspace 7
bindsym $mod+8 workspace 8
bindsym $mod+9 workspace 9
bindsym $mod+0 workspace 10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace 1
bindsym $mod+Shift+2 move container to workspace 2
bindsym $mod+Shift+3 move container to workspace 3
bindsym $mod+Shift+4 move container to workspace 4
bindsym $mod+Shift+5 move container to workspace 5
bindsym $mod+Shift+6 move container to workspace 6
bindsym $mod+Shift+7 move container to workspace 7
bindsym $mod+Shift+8 move container to workspace 8
bindsym $mod+Shift+9 move container to workspace 9
bindsym $mod+Shift+0 move container to workspace 10

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

bindsym $mod+Shift+minus move scratchpad
bindsym $mod+minus scratchpad show

# quickswitch-i3
# bindsym Mod1+Shift+space exec quickswitch.py --dmenu 'dmenu -i -l 5 -nb "#222222" -nf "#888888" -sb "#5f87af" -sf "#eeeeee"'

# Screenshots with scrot
## Select region
# bindsym --release Mod1+Shift+4 exec --no-startup-id ~/bin/screenshot select
## All displays stitched together
# bindsym --release Mod1+Shift+3 exec --no-startup-id ~/bin/screenshot both
## Focused display (can't be done with scrot, xfce4-screenshooter)
## TRY:
# 1. Take --multidisp
# 2. Get focused display (via i3-msg (focused workspace)?)
# 3. Crop using dimensions takes from #2.
# bindsym --release Mod1+Shift+3 exec --no-startup-id ~/bin/screenshot focused

# WM_CLASS(STRING) = "AAA", "BBB"
# WM_WINDOW_ROLE(STRING) = "CCC"
# WM_NAME(UTF8_STRING) = "DDD"
# _NET_WM_NAME(UTF8_STRING) = "DDD"
# _NET_WM_WINDOW_TYPE = "EEE"
# instance="AAA"
# class="BBB"
# window_role="CCC"
# title="DDD"
# window_type="EEE" (normal, dialog, utility, toolbar, splash, menu, dropdown_menu, popup_menu, tooltip, notification)

for_window [title="music - ncmpcpp"] move scratchpad
bindsym $mod+m [title="music - ncmpcpp"] scratchpad show

for_window [class="KeePassXC" window_type="normal"] move scratchpad
bindsym Mod1+shift+p [class="KeePassXC" window_type="normal"] scratchpad show

for_window [class="Emacs" title="^agenda$"] move scratchpad
bindsym $mod+o [class="Emacs" title="^agenda$"] scratchpad show

for_window [instance="devdocs.io" window_role="pop-up"] move scratchpad
bindsym $mod+d [instance="devdocs.io" window_role="pop-up"] scratchpad show

for_window [class="zest" window_role="browser-window"] move scratchpad
# bindsym $mod+d [class="zest"] scratchpad show

# todoist.com in a Chromium instance
for_window [class="Chromium-browser" instance="todoist.com"] move scratchpad
bindsym Mod1+shift+t [class="Chromium-browser" instance="todoist.com"] scratchpad show

# toggl.com time tracker in a Chromium instance
# for_window [class="Chromium-browser" instance="toggl.com__app"] move scratchpad
# bindsym Mod1+shift+t [class="Chromium-browser" instance="toggl.com__app"] scratchpad show

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 5 px or 5 ppt
        bindsym j resize grow height 5 px or 5 ppt
        bindsym k resize shrink height 5 px or 5 ppt
        bindsym l resize grow width 5 px or 5 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
        bindcode Control+34 mode "default"
}

bindsym $mod+r mode "resize"

bindsym $mod+Left resize shrink width 5px or 5 ppt
bindsym $mod+Down resize grow height 5px or 5 ppt
bindsym $mod+Up resize shrink height 5px or 5 ppt
bindsym $mod+Right resize grow width 5px or 5 ppt

bar {
        id primary_bar
        tray_output none
        status_command i3blocks -c ~/.config/i3/i3blocks.conf
        modifier none
        mode dock
        wheel_up_cmd nop
        wheel_down_cmd nop

        colors {
               # background #000000
               # statusline #ffffff
               # focused_workspace    #ffffff #ffffff #000000
               # inactive_workspace   #222222 #222222 #888888
               # urgent_workspace     #e74c3c #e74c3c #111111
               background #ffffff
               statusline #444444
               focused_workspace    #444444 #444444 #ffffff
               active_workspace     #aaaaaa #aaaaaa #111111
               inactive_workspace   #dddddd #dddddd #444444
               urgent_workspace     #e74c3c #e74c3c #111111
        }
}

bar {
    id secondary_bar
    tray_output HDMI-2
    position bottom
    mode hide
    modifier Mod1
    status_command i3blocks -c ~/.config/i3/i3blocks-bar2.conf
    colors {
               # background #000000
               # statusline #ffffff
               # focused_workspace    #ffffff #ffffff #000000
               # inactive_workspace   #222222 #222222 #888888
               # urgent_workspace     #e74c3c #e74c3c #111111
               background #ffffff
               statusline #444444
               focused_workspace    #444444 #444444 #ffffff
               active_workspace     #aaaaaa #aaaaaa #111111
               inactive_workspace   #dddddd #dddddd #444444
               urgent_workspace     #e74c3c #e74c3c #111111
    }
}

bindsym $mod+b bar mode toggle primary_bar

assign [instance="trello.com" class="Chromium" window_role="pop-up"] 4
assign [class="Deluge" window_type="normal"] 8
assign [class="ViberPC"] 10
assign [title="plaidchat"] 10
assign [title="Slack"] 10

# Floaters
# for_window [class="Xfce4-notifyd"] floating enable border 1pixel
for_window [class="Firefox" window_role="About"] floating enable
for_window [class="Firefox" window_role="SessionPrompt"] floating enable
for_window [class="firefox" title="Sign in - Google Accounts - Mozilla Firefox"] floating enable

# Thunar file operation progress
for_window [class="Thunar" title="File Operation Progress"] floating enable

# Pinboard extension
for_window [class="Firefox" title="about:blank -"] floating enable
for_window [class="Firefox" title="https://pinboard.in - Pinboard - Save a Bookmark"] floating enable

for_window [instance="^bashrun2-run-dialog$" class="^XTerm$"] border 1pixel floating enable resize shrink height 10px
for_window [class="Wicd-client.py"] floating enable

for_window [class="Qalculate"] floating enable
for_window [class="net-pms-PMS" title="^Universal Media Server"] floating enable

for_window [class="Icedove" instance="Msgcompose"] floating enable
for_window [class="Icedove" title="Address Book"] floating enable
for_window [class="Icedove" title="Activity Manager"] floating enable
for_window [title="Icedove Preferences"] floating enable
for_window [title="Firefox Preferences"] floating enable

for_window [instance="key-mon"] floating enable
for_window [instance="gromit"] floating enable
for_window [instance="gromit-mpx"] floating enable
for_window [class="Pavucontrol"] floating enable
for_window [title="Tree Style Tab Configuration"] floating enable
for_window [instance="android-file-transfer"] floating enable
for_window [class="Blueman-manager"] floating enable
for_window [instance="system-config-printer.py"] floating enable

# #3465a4
# #5F87AF
# #708CA2
# 528CCB
# Colors
# class                 border  backgr. text    indicator child_border
# client.focused          #5f87af #5f87af #ffffff #5f87af
# client.focused          #4682b4 #4682b4 #ffffff #4682b4
# client.focused          #4682b4 #4682b4 #ffffff #4682b4
client.focused          #ffffff #ffffff #000000 #ffffff  #ffffff
# client.focused_inactive #444444 #444444 #ededed #292d2e
client.focused_inactive #444444 #444444 #ededed #292d2e #ffffff
# client.focused_inactive #444444 #444444 #ededed #292d2e
client.unfocused        #444444 #444444 #ededed #292d2e #ffffff
# client.unfocused        #444444 #444444 #ededed #292d2e
client.urgent           #e74c3c #e74c3c #111111 #e74c3c

default_border none
gaps inner 6
smart_gaps on
# smart_borders no_gaps

for_window [class="Emacs"] border pixel 6

# exec dropbox start
# exec ~/bin/menutray -g -i
# exec wicd-gtk --tray
# exec --no-startup-id compton -b
# exec --no-startup-id emacs --daemon # && ~/bin/agenda
# exec --no-startup-id viber
# exec --no-startup-id keepassxc
# exec --no-startup-id flameshot
# exec --no-startup-id feh --bg-fill ~/Pictures/desktop-backgrounds/current
# exec --no-startup-id pasystray

for_window [title="ssh-login"] floating enable
# exec ~/bin/maybe-floating-ssh-login

workspace 1  output HDMI-2
workspace 2  output HDMI-2
workspace 3  output HDMI-2
workspace 4  output HDMI-2
workspace 5  output HDMI-2
workspace 6  output HDMI-2
workspace 7  output DVI-D-1
workspace 8  output DVI-D-1
workspace 9  output DVI-D-1
workspace 10 output DVI-D-1
